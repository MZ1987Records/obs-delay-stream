name: build

on:
  push:
    tags:
      - "v*.*.*"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write
    env:
      OBS_STUDIO_REF: master
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install markdown PDF tools
        shell: pwsh
        run: |
          npm install --no-save md-to-pdf github-markdown-css

      - name: Convert README to PDF
        shell: pwsh
        run: |
          $src = "$env:GITHUB_WORKSPACE/README.md"
          $dist = "$env:GITHUB_WORKSPACE/dist"
          $dst = "$dist/README.pdf"
          $css = "$env:GITHUB_WORKSPACE/node_modules/github-markdown-css/github-markdown-light.css"
          if (-Not (Test-Path $src)) { throw "README.md not found." }
          if (-Not (Test-Path $css)) { throw "github-markdown CSS not found: $css" }
          New-Item -ItemType Directory -Force $dist | Out-Null
          npx md-to-pdf $src --dest $dist --stylesheet $css --body-class "markdown-body"
          if (-Not (Test-Path $dst)) { throw "PDF was not generated: $dst" }

      - name: Detect Windows SDK
        id: sdk
        shell: pwsh
        run: |
          $sdkRoot = "C:\\Program Files (x86)\\Windows Kits\\10\\Lib"
          $dirs = Get-ChildItem $sdkRoot -Directory | Select-Object -ExpandProperty Name
          if (-Not $dirs) { throw "Windows SDK not found: $sdkRoot" }
          $ver = $dirs | ForEach-Object { [version]$_ } | Sort-Object -Descending | Select-Object -First 1
          $verStr = $ver.ToString()
          Add-Content $env:GITHUB_ENV "WINSDK_VERSION=$verStr"
          Add-Content $env:GITHUB_OUTPUT "version=$verStr"

      - name: Resolve OBS ref
        id: obs
        shell: pwsh
        run: |
          $sha = (git ls-remote https://github.com/obsproject/obs-studio.git "refs/heads/$env:OBS_STUDIO_REF" | ForEach-Object { ($_ -split '\s+')[0] } | Select-Object -First 1)
          if (-Not $sha) { throw "Failed to resolve OBS ref: $env:OBS_STUDIO_REF" }
          Add-Content $env:GITHUB_ENV "OBS_STUDIO_SHA=$sha"
          Add-Content $env:GITHUB_OUTPUT "sha=$sha"
          Write-Host "OBS ref $env:OBS_STUDIO_REF => $sha"

      - name: Prepare third_party
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force third_party | Out-Null

      - name: Clone OBS Studio
        shell: pwsh
        run: |
          if (-Not (Test-Path "third_party/obs-studio")) {
            git clone --filter=blob:none --no-checkout https://github.com/obsproject/obs-studio.git third_party/obs-studio
          }
          Set-Location third_party/obs-studio
          git fetch --depth 1 origin $env:OBS_STUDIO_SHA
          git checkout --force $env:OBS_STUDIO_SHA
          git submodule update --init --recursive --depth 1

      - name: Restore OBS build cache
        id: obs-cache
        uses: actions/cache@v4
        with:
          path: third_party/obs-studio/build_x64
          key: ${{ runner.os }}-obs-build-${{ steps.obs.outputs.sha }}-sdk-${{ steps.sdk.outputs.version }}
          restore-keys: |
            ${{ runner.os }}-obs-build-${{ steps.obs.outputs.sha }}-sdk-
            ${{ runner.os }}-obs-build-${{ steps.obs.outputs.sha }}-
            ${{ runner.os }}-obs-build-

      - name: Patch OBS SDK version
        shell: pwsh
        run: |
          Set-Location third_party/obs-studio
          $presetPath = "CMakePresets.json"
          if (-Not (Test-Path $presetPath)) { throw "CMakePresets.json not found." }
          $raw = Get-Content $presetPath -Raw
          $patched = $raw.Replace("10.0.22621.0", $env:WINSDK_VERSION)
          if ($raw -eq $patched) { Write-Host "No preset SDK version to replace." }
          $patched | Set-Content $presetPath -Encoding UTF8

      - name: Check OBS build outputs
        id: obs-build-check
        shell: pwsh
        run: |
          $libobs = "third_party/obs-studio/build_x64/libobs/RelWithDebInfo/obs.lib"
          if (Test-Path $libobs) {
            Write-Host "OBS build cache hit: $libobs"
            Add-Content $env:GITHUB_OUTPUT "skip=true"
          } else {
            Write-Host "OBS build cache miss: $libobs"
            Add-Content $env:GITHUB_OUTPUT "skip=false"
          }

      - name: Build OBS Studio
        if: steps.obs-build-check.outputs.skip != 'true'
        shell: pwsh
        run: |
          Set-Location third_party/obs-studio
          cmake --list-presets
          cmake --preset windows-x64
          cmake --build --preset windows-x64 --config RelWithDebInfo --parallel

      - name: Clone websocketpp
        shell: pwsh
        run: |
          if (-Not (Test-Path "third_party/websocketpp")) {
            git clone https://github.com/zaphoyd/websocketpp.git third_party/websocketpp
          }

      - name: Clone asio
        shell: pwsh
        run: |
          if (-Not (Test-Path "third_party/asio")) {
            git clone --branch asio-1-18-2 --depth 1 https://github.com/chriskohlhoff/asio.git third_party/asio
          }

      - name: Configure plugin (ProgramData layout)
        shell: pwsh
        run: |
          cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DOBS_SOURCE_DIR="$env:GITHUB_WORKSPACE/third_party/obs-studio" -DOBS_PLUGIN_LEGACY_INSTALL=OFF

      - name: Build plugin
        shell: pwsh
        run: |
          cmake --build build --config RelWithDebInfo --parallel

      - name: Install to dist (ProgramData layout)
        shell: pwsh
        run: |
          $dist = "$env:GITHUB_WORKSPACE/dist"
          New-Item -ItemType Directory -Force $dist | Out-Null
          $prefix = "$dist/For ProgramData"
          cmake --install build --config RelWithDebInfo --prefix $prefix

      - name: Configure legacy layout
        shell: pwsh
        run: |
          cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DOBS_SOURCE_DIR="$env:GITHUB_WORKSPACE/third_party/obs-studio" -DOBS_PLUGIN_LEGACY_INSTALL=ON

      - name: Install to dist (Program Files legacy layout)
        shell: pwsh
        run: |
          $dist = "$env:GITHUB_WORKSPACE/dist"
          New-Item -ItemType Directory -Force $dist | Out-Null
          $prefix = "$dist/For Program Files (legacy)"
          cmake --install build --config RelWithDebInfo --prefix $prefix

      - name: Pack artifact
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $zip = "obs-delay-stream-$tag.zip"
          if (Test-Path $zip) { Remove-Item $zip -Force }
          Compress-Archive -Path "dist\\*" -DestinationPath $zip

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: obs-delay-stream
          path: obs-delay-stream-${{ github.ref_name }}.zip

      - name: Create draft release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          files: obs-delay-stream-${{ github.ref_name }}.zip
